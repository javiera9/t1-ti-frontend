# URL publica de la API del backend. No es secreta (es solo el endpoint), pero
# nunca pongas aqui tokens, secrets ni credenciales: esas viven solo en el backend.
VITE_API_BASE_URL=http://localhost:8000
